apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: dashy
  namespace: flux-system
spec:
  image: nicolargo/glances
  interval: 2h # Checks every 2 hours to avoid Docker Hub limits
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: dashy
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: glances
  filterTags:
    pattern: "^latest$"
  interval: 2h
  digestReflectionPolicy: Always
  policy:
    alphabetical:
      order: asc
